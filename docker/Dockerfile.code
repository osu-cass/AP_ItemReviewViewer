FROM tomcat:7-jre7

WORKDIR /usr/local/tomcat/
ARG contextName=ROOT

COPY context.xml ./conf
RUN  rm -rf webapps/*

COPY itemreviewviewer.war ./webapps
RUN mv ./webapps/itemreviewviewer.war ./webapps/$contextName.war

ENTRYPOINT ["catalina.sh", "run"]